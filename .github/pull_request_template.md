## What this changes

<!-- One or two sentences. The CHANGELOG entry gets written from this at merge, so say what a user would notice. -->

Resolves #

## Checks

- [ ] `npm run lint`
- [ ] `npm run test:unit`
- [ ] `npm run test:package`
- [ ] `npm run test:integration` (on Linux: `xvfb-run -a npm run test:integration`)

Platform tested on:

<!-- Windows and Linux behave differently for shell selection, profile paths and the uninstall command. Say which one you ran. -->

## Notes

<!-- Anything a reviewer would otherwise have to work out: a deliberate omission, a trade-off, something you were unsure about. Delete this section if there is nothing. -->

<!-- Do not add a CHANGELOG.md entry: the maintainer writes it at merge, so parallel pull requests do not collide. See CONTRIBUTING.md. -->
